{
  "name": "@orchestrator/bus-client",
  "version": "0.1.0",
  "description": "Shared Redis Streams bus client for all orchestrator services",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}