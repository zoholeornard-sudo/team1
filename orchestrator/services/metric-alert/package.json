{
  "name": "@orchestrator/metric-alert",
  "version": "0.1.0",
  "description": "Metric alert service — monitors MBO metrics, emits alerts, triggers manager actions",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@orchestrator/contracts": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}