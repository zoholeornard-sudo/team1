{
  "name": "@orchestrator/workflow",
  "version": "0.1.0",
  "description": "Workflow service — first-class Plan/Workflow entity for task decomposition and state management",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@orchestrator/contracts": "workspace:*",
    "duckdb": "^1.1.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}