{
  "name": "@orchestrator/manager-loop",
  "version": "0.1.0",
  "description": "Manager loop service — recurring coordination, heartbeat monitoring, conflict resolution",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@orchestrator/contracts": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}